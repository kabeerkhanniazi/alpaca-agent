# Secrets — never commit
.env
.env.local

# Virtualenv
venv/
.venv/

# Vendored upstream clone (pulled via setup instructions, not committed)
ta-base/

# Runtime output
logs/
# Runtime data is local-only, except the committed journal snapshot the
# deployed dashboard falls back to when it cannot reach the live data branch.
data/*
!data/snapshot/
results/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/

# WSL / Windows cruft
*:Zone.Identifier
.DS_Store

# The design mockup lives at docs/design/alpaca.html; this is the working copy.
alpaca.html
