# Broker + market data: Alpaca's official CLI (`alpaca`), installed separately with
#   go install github.com/alpacahq/cli/cmd/alpaca@latest
# The alpaca-py SDK is deliberately absent — the hackathon requires Alpaca's MCP
# server or CLI tools, and a direct SDK dependency does not satisfy that.

# Graph orchestration (deterministic nodes; no LLM in this agent)
langgraph>=1.2.0

# Data handling
pandas>=2.2.0
numpy>=1.26.0

# Config
python-dotenv>=1.0.0

# Dashboard
streamlit>=1.30.0
plotly>=5.18.0

# Tests
pytest>=7.4.0
pytest-mock>=3.12.0
